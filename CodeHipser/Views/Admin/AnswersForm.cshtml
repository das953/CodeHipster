@*@model CodeHipser.ViewModels.QuestionViewModel
@{
    ViewData["Title"] = "QuestionForm";
}

<h2>@Model.QuestionText</h2>
<p>Answers: </p>
<form asp-action="SaveAnswers" method="post" asp-antiforgery="true">
    <div id="answers-container">
        <button type="submit" class="btn btn-primary">Save</button>
        <button id="add-answer" class='btn btn-primary'>Add Answer</button>
        @for(int i=0; i<Model.Answers.Count; i++)
        {
            bool isCorrect = Model.Answers[i].IsCorrect;
            <div class="form-group radio">
                @if(i==Model.CorrectAnswerId)
                {
                    <input asp-for="CorrectAnswerId" value="@i" type="radio" checked>
                }
                else
                {
                    <input asp-for="CorrectAnswerId" value="@i" type="radio">
                }
                <textarea asp-for="Answers[i].AnswerText" class="form-control"></textarea>
            </div>
        }
        <input asp-for="Id" type="hidden">
        <input asp-for="QuestionText" type="hidden">
        
    </div>
</form>
@section Scripts{
    <script asp-src-include="lib/jquery-validation/dist/jquery.*.min.js"></script>
    <script asp-src-include="lib/jquery-validation-unobtrusive/*.min.js"></script>
    <script>
        $(document).ready(function () {
            var itemCount = @Model.Answers.Count;
            $("#add-answer").click(function () {
                var itemName = "Answers[" + itemCount + "].AnswerText";
                var itemId = "Answers_" + itemCount + "__AnswerText";
                var item = $("<div class='radio'><input name='CorrectAnswerId' type='radio' value='" + itemCount + "'><textarea name='" + itemName + "' class='form-control' id='" + itemId + "'></textarea></div>");
                $("#answers-container").append(item);
                itemCount++;
            });
        });
    </script>
}*@

